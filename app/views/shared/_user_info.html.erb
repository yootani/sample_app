<a href="<%= user_path(current_user) %>">
  <%= gravatar_for current_user, size: 52 %>
</a>
<h1>
  <!-- View userName -->
  <%= current_user.name %>
</h1>
<span>
  <!--  -->
  <%= link_to "view my profile", current_user %>
</span>
<span>
  <!-- 1micropost or 2,3micropost's' modify -->
  <%= pluralize(current_user.microposts.count, "micropost") %>
</span>
